import { AiOutlineMenu } from "react-icons/ai";
import styled from "styled-components";

const MenuBar = () => {
  return (
    <div>
      <AiOutlineMenu />
    </div>
  );
};
export default MenuBar;
